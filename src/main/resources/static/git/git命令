查看所有分支：git branch -a
git删除本地分支：git branch -d xxxxx
    当一个分支被推送并合并到远程分支后，-d 才会本地删除该分支。如果一个分支还没有被推送或者合并，那么可以使用-D强制删除它。
git删除远程分支：git push origin --delete xxxxx
同步远程分支：git fetch
选择分支：git checkout master
合并分支：git meger xxx
