1、设置Git的user name和email
git config --global user.name "tom"
git config --global user.email "tom@gmail.com"
2、生成密钥(linux和windows一样)
ssh-keygen -t rsa -C "tom@gmail.com"
3、在~/.ssh文件目录下查看公钥
cat id_rsa.pub
-------------------------
git删除远程分支
git push origin --delete revert-2-master










登录命令：mysql -u debian-sys-main -p
mysql 服务命令：
service mysql status
service mysql start
service mysq restart
service mysql stop